<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="opr.capacidad">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <protected-broadcast android:name="android.intent.action.MEDIA_MOUNTED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TareaElegirImagen1Activity"
            android:label="Elegir imagen correcta" />
        <activity
            android:name=".ElegirTarea"
            android:label="Elegir tarea" />
        <activity android:name=".ResolverTareaActivity" />
        <activity android:name=".RegistrarUsuario" />
        <activity android:name=".PantallaPrincipal" />
        <activity android:name=".ConsultarUsuario" />
        <activity
            android:name=".TodoUsuario"
            android:label="@string/title_activity_todo_usuario"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>